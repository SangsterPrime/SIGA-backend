package cl.duoc.siga.backend.enums;

/** Resultado de una revisión (usado por DeclaracionSag y RevisionPdi). */
public enum ResultadoRevision {
    PENDIENTE,
    APROBADA,
    OBSERVADA,
    RECHAZADA
}
